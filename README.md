# 2v2_sachmatai

## Run backend and frontend separately

From the workspace root, open two terminals.

Backend:

```powershell
cd Backend
npm run dev
```

Frontend:

```powershell
cd Frontend
npm run dev
```
# Once done you can open frontentend localhost server and use the system
